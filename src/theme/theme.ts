import { createTheme } from "@mui/material";

const themeOptions = {
  palette: {
    // type: "light",
    // primary: {
    //   main: "#f0e4d3",
    // },
    secondary: {
      main: "#8c7a83",
    },
    background: {
      default: "#f0e4d3",
    },
  },
};
export default createTheme(themeOptions);
